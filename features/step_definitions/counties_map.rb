Given("the user on the rep search page") do |map|
Map.create()
end

When("the user clicks on a county on the map") do ||

end

Then("the user should be redirected to the search results page for that county") do ||

end

Then("the page should display representatives for that county") do ||

end
